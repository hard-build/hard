description: Reuse one production object in two GoogleTest binaries.

steps:
  - test:
      sources:
        - .
      compiled_once:
        - counter.cpp
      binaries:
        increment.test:
          passed: 3
        reset.test:
          passed: 2
