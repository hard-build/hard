description: Discover a production calculator and run five GoogleTest cases.

steps:
  - test:
      sources:
        - calculator.test.cpp
      compiled_once:
        - calculator.cpp
      binaries:
        calculator.test:
          passed: 5
